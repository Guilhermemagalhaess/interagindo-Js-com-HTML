let titulo = document.querYSelector ("") para incluir titulo
titulo.innerHTML = ""

let paragrafo = document.querySelector("")
paragrafo.innerHTML = ""

criar função: function   é um trecho de códigoque executa ou tem alguma responssabilidade
function nome() {
    
}


OBS: Boa prática evitar repetição de códigos


//let titulo = document.querySelector("h1");
//titulo.innerHTML = "Hora do desafio";

//let paragrafo = document.querySelector ("p");
//paragrafo.innerHTML= "Escolha um número entre 1 e 10";

function exibirTextoNaTela(tag, texto) {
    let campo = document.querySelector(tag);
    campo.innerHTML = texto;
}

exibirTextoNaTela ("h1", "Hora do desafio");
exibirTextoNaTela('p', 'Escolha um número entre 1 e 10');

function verificarChute() {
    console.log("Cite uma cidade brasileira");
    }
Eliminando linhas...

return para uma variável retornar a alguma função.



Atribuir funções para as variáveis com return

let numeroSecreto = gerarNumeroAleatorio();

function exibirTextoNaTela(tag, texto) {
    let campo = document.querySelector(tag);
    campo.innerHTML = texto;
}

exibirTextoNaTela ("h1", "Hora do desafio");
exibirTextoNaTela('p', 'Escolha um número entre 1 e 10');

function verificarChute() {
    console.log(numeroSecreto);
    }

function gerarNumeroAleatorio(params) {
  return parseInt(Math.random() * 10 + 1);
}





Tipo booleano = verdadeiro ou false

let numeroSecreto = gerarNumeroAleatorio();

function exibirTextoNaTela(tag, texto) {
    let campo = document.querySelector(tag);
    campo.innerHTML = texto;
}

exibirTextoNaTela ("h1", "Hora do desafio");
exibirTextoNaTela('p', 'Escolha um número entre 1 e 10');

function verificarChute() {
    (((((((((((let chute = document.querySelector ("input").value;))))))))))))))
    console.log(chute == numeroSecreto);
    }

function gerarNumeroAleatorio(params) {
  return parseInt(Math.random() * 10 + 1);
}
